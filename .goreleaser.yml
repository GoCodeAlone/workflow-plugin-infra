version: 2
project_name: workflow-plugin-infra
before:
  hooks:
    - go mod tidy
    - "sed -i.bak -e 's/\"version\": \".*\"/\"version\": \"{{ .Version }}\"/' -e 's#/releases/download/v[^/]*/#/releases/download/v{{ .Version }}/#g' -e 's/workflow-plugin-infra_[0-9][0-9A-Za-z.~-]*/workflow-plugin-infra_{{ .Version }}/g' plugin.json && rm -f plugin.json.bak"
builds:
  - id: workflow-plugin-infra
    main: ./cmd/workflow-plugin-infra
    binary: workflow-plugin-infra
    env:
      - CGO_ENABLED=0
      - GOPRIVATE=github.com/GoCodeAlone/*
    ldflags:
      - "-s -w -X github.com/GoCodeAlone/workflow-plugin-infra/internal.Version={{ .Version }}"
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
archives:
  - id: archive
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - plugin.json
      - plugin.contracts.json
checksum:
  name_template: "checksums.txt"
release:
  github:
    owner: GoCodeAlone
    name: workflow-plugin-infra
